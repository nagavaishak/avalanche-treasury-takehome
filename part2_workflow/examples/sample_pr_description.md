# [promoteit] Promote `validator_churn_hourly` to production

> Generated by promoteit v0.4.2 from `pipeline_spec.yaml` (commit a1b2c3d).
> Template: `airflow_dag_batch@2.1.0`. LLM model pin: `claude-sonnet-4-6`.

## Summary

This PR promotes `exploratory/eric/validator_churn/` to a production Airflow DAG. It runs hourly, computes net validator-set changes on Avalanche P-Chain, writes to `treasury_marts.validator_churn_hourly`, and surfaces three DQ checks.

## What I generated

| Artifact | Path | Lines |
|---|---|---|
| Airflow DAG | `dags/validator_churn_hourly.py` | 218 |
| Pipeline module | `pipelines/validator_churn_hourly/main.py` | 142 |
| Schema contract | `contracts/validator_churn_hourly.schema.json` | 36 |
| Unit tests (scaffold) | `tests/pipelines/test_validator_churn_hourly.py` | 87 |
| DQ checks | `dq/validator_churn_hourly.sql` | 41 |
| Documentation | `docs/pipelines/validator_churn_hourly.md` | 23 |

## What I reused

- `utilities.dune_client.DuneClient` — matched against `fetch_dune_data()` in the exploratory code (high confidence)
- `utilities.postgres_upsert.upsert_dataframe` — matched against `df.to_sql(..., if_exists='append')` (medium confidence; **please verify line 89**)
- `utilities.secrets_manager.get_secret` — replaced the hardcoded RPC URL on line 12 of the notebook

## Where I am uncertain

These need explicit human review before merge:

1. **`fetch_validator_set` (DAG line 47).** I matched this to `utilities.chain_rpc.get_validators`, but their signatures differ: the utility expects a `block_height: int`, but the exploratory code passes `block_height: str`. I generated a cast at the call site; please confirm this is the correct semantic (and not, e.g., a deliberate string-keyed lookup against a different API).
2. **Unit tests assert current output.** The exploratory code's outputs were captured as fixtures and the generated tests assert against them. If the exploratory code has bugs, these tests will perpetuate them. **Please review every assertion in `tests/pipelines/test_validator_churn_hourly.py` and confirm correctness manually.**
3. **DQ thresholds.** I copied `dq_checks.threshold` values directly from the spec. The spec author set them, but the appropriateness of the numbers for *production* data has not been validated. Tune after the first production run.

## Where the spec was ambiguous

- The spec specifies `freshness.sla: 1h` but the cron is `"5 * * * *"`. This gives a worst-case 1h5m lag, exceeding the SLA. **Suggested action:** change cron to `"*/30 * * * *"` to give two attempts per hour, or relax the SLA to `2h`. I did NOT change either — this is a judgment call.

## Human review required

These items remain mandatory regardless of CI status:

- [ ] Every `assert` line in the generated tests
- [ ] The `purpose:` block of the spec for accuracy
- [ ] DQ thresholds for production-appropriateness
- [ ] The signature-cast at DAG line 47 (or rejection of the utility match)
- [ ] Cron vs SLA mismatch noted above

## High-risk file flags

This PR does not touch `/utilities/`, `migrations/`, `dbt/models/marts/`, `*.dq.*`, or `.github/workflows/`. **Single-reviewer merge is sufficient.**

## CI status

(Filled in by GitHub Actions.)

## How to reproduce

```bash
git checkout promoteit/validator-churn-hourly
gh pr checkout 4123
promoteit replay --spec pipeline_spec.yaml --dry-run
# Should produce a no-op diff. If not, regenerate with `promoteit promote` and amend.
```
